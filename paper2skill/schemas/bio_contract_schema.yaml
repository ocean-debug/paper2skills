required:
  - bio_contract
properties:
  bio_contract:
    required:
      - modality
      - organism
      - input_matrix_state
      - metadata_requirements
      - modality_contracts
      - interpretation_boundary
