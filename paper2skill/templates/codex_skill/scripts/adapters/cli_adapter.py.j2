from __future__ import annotations

from pathlib import Path


def run(manifest: dict, out: Path) -> dict:
    return {
        "status": "not_implemented",
        "adapter_type": "cli",
        "message": "CLI execution requires a verified command mapping and is not auto-generated.",
    }
