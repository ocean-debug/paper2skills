from __future__ import annotations

from pathlib import Path


def run(manifest: dict, out: Path) -> dict:
    return {
        "status": "blocked",
        "adapter_type": "cli",
        "message": "CLI adapter is candidate-only and requires user-reviewed command wiring.",
    }
