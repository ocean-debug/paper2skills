from __future__ import annotations

from pathlib import Path


def run(manifest: dict, out: Path) -> dict:
    return {
        "status": "blocked",
        "adapter_type": "notebook",
        "message": "Notebook adapter is candidate-only and is not run automatically.",
    }
