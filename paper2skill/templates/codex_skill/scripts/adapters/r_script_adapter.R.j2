run_adapter <- function(manifest_path, out_dir) {
  list(
    status = "not_implemented",
    adapter_type = "r_script",
    message = "R script execution requires user-reviewed adapter wiring."
  )
}
