status: user_confirmed_gold_revision
notes:
  - category: source_basis
    reason: Gold has been revised from user-provided paper, repository, README, dependency, CLI, IO/Bio, and adapter notes.
    action: Treat README as the primary tutorial evidence without requiring exact heading anchors.
  - category: paired_inputs
    reason: Gold expects Ribo-seq and RNA-seq raw count matrices plus a sample information file in the same sample order.
    action: Preserve genes-by-samples orientation and Condition/SeqType/Batch metadata requirements.
  - category: dependency_contract
    reason: Required library evidence is DESeq2 and ggplot2.
    action: Do not score apeglm as required unless later manually reconfirmed.
  - category: adapter_status
    reason: expected_initial_status is now verified because the Rscript invocation is explicitly documented.
    action: Future live adapter checks should verify small input execution separately from static extraction scoring.
