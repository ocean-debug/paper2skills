expected_adapter_type: cli
expected_initial_status: verified
reason: DTEG.R has a documented Rscript CLI invocation and bundled sample-data
  result convention for reviewed execution.
demo_run:
  allowed: true
  expected_behavior: run_with_validated_small_ribo_rna_count_matrices_and_sample_info
non_demo_run:
  expected_behavior: validate_raw_counts_and_sample_info_before_execution
must_not:
- run_without_validating_raw_counts
- run_if_sample_info_missing_required_columns
- auto_install_DESeq2_or_ggplot2
