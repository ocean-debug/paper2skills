status: user_confirmed_gold_revision
notes:
  - category: source_basis
    reason: Gold has been revised from user-provided paper, repository, README Usage, dependency, IO/Bio, and adapter notes.
    action: Keep README Usage as the primary tutorial evidence for current scoring.
  - category: adapter_status
    reason: expected_initial_status is now verified because the package API and demo workflow are documented.
    action: Future live adapter checks should verify dependencies and small demo execution separately from static extraction scoring.
  - category: io_contract
    reason: Gold accepts both genes-by-cells matrix plus metadata and Seurat object input with sc@meta.data columns.
    action: Preserve both metadata alias sets: cell.type/condition for matrix input and cell_type/label for Seurat input.
