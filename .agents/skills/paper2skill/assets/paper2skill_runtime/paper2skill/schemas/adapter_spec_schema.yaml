required:
  - adapter_type
  - status
  - entrypoint
  - command
  - module
  - function
  - evidence
  - caveats
properties:
  adapter_type:
    enum: [demo_only, python_api, cli, notebook, r_script, workflow_engine]
  status:
    enum: [dry_run_only, verified]
